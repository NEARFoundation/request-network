{
    "name": "server",
    "version": "1.0.0",
    "description": "near utility server",
    "main": "app.js",
    "scripts": {
        "start": "env-cmd -f ../neardev/dev-account.env nodemon app.js --config ./nodemon.json",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "license": "UNLICENSED",
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.17.1",
        "pg": "^8.6.0",
        "node-cron": "^3.0.0"
    },
    "devDependencies": {
        "nodemon": "^2.0.7",
        "env-cmd": "~10.1.0"
    }
}
